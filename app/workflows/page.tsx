import type { Metadata } from 'next'
import Link from 'next/link'
import { ArrowRight, Check, CircleDot, Workflow as WorkflowIcon } from 'lucide-react'
import { CTA, Eyebrow, PageIntro, WorkflowVisual, workflowSteps } from '@/components/site'
export const metadata:Metadata={title:'Workflows',description:'Explore practical, human-centered AI workflows from Flowmint AI.'}
export default function Workflows(){return <><PageIntro eyebrow="AI WORKFLOWS" title="A clear path from busywork to better work." copy="A workflow connects small actions into a system you can understand, use, and improve. AI can help with the repeatable parts; you keep the judgment."/><section className="section-wrap workflow-page-visual"><WorkflowVisual/></section><section className="workflow-principles"><div className="section-wrap"><div className="section-heading"><Eyebrow>BUILT FOR REAL WORK</Eyebrow><h2>Simple steps. Thoughtful automation.</h2></div><div className="workflow-principle-grid">{[{i:'01',t:'Start with a real task',d:'Pick the work that repeats and takes more effort than it should.'},{i:'02',t:'Make the flow visible',d:'Give each step a clear purpose and a useful handoff.'},{i:'03',t:'Keep a person in the loop',d:'Use AI for assistance; use human judgment for decisions and approval.'}].map(x=><article key={x.i}><span>{x.i}</span><h3>{x.t}</h3><p>{x.d}</p></article>)}</div></div></section><section className="section-wrap workflow-example"><div><Eyebrow>WORKFLOW 001 · CONTENT</Eyebrow><h2>From a rough idea to a post that sounds like you.</h2><p>Use this example as a starting point. The tools may change; a thoughtful process still works.</p><Link className="button button-dark" href="/products/linkedin-content-automation">Explore LinkedIn Content Automation <ArrowRight size={16}/></Link></div><div className="compact-flow">{workflowSteps.map((s,i)=><div key={s}><span>{i<4?<CircleDot size={16}/>:<Check size={16}/>}</span>{s}</div>)}</div></section><CTA/></>}
